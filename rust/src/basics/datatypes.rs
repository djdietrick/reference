// Scalar Types (or basic types)

// Integer, either signed or unsigned, bits in (8,16,32,64,128)
let num: i32 = -5; 
let unsigned_num: u32 = 5;

// Floats, either 32 or 64 bit
let f = 5.0; // Defaults to f64
let f: f32 = 3.9;

// Boolean
let b: bool = true;

// Char, specified by single-quotes
let c: char = 'c';
